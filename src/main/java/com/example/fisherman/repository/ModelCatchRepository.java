package com.example.fisherman.repository;

import com.example.fisherman.model.ModelCatch;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ModelCatchRepository extends JpaRepository<ModelCatch,Long> {
}
